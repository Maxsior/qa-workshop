import styled from 'styled-components';

const ContentWrapper = styled.main`
    margin: var(--offset-xl) auto;
    width: 400px;
    display: flex;
    flex-direction: column;
    align-items: stretch;
`;

export default ContentWrapper;
